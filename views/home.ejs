<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Home</title>
    <link rel="stylesheet" href="/public/styles/home.css">
</head>
<body>
    <%- include("navbar") %>

    <div class="cards">
        <% blogs.forEach(blog => { %>
            <div class="card">
                <% if(blog.image) { %>
                    <img src="<%= blog.image %>" alt="<%= blog.title %>" width="250">
                <% } %>
                <h2><%= blog.title %></h2>
                <h4><%= blog.subTitle %></h4>
                <p><%= blog.description %></p>
                <a href="/blog/<%= blog.id %>">See more</a>
            </div>
        <% }) %>
    </div>

    <%- include("footer") %>
</body>
</html>
